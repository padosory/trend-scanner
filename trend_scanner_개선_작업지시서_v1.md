# trend_scanner 개선 작업지시서 (3-모델 병합본 v1)

> **이 문서의 목적**: Claude / Gemini / ChatGPT 3개 분석을 대조·병합한 **실행용 작업지시서**.
> 리뷰 글이 아니라 Claude Code가 바로 착수할 수 있게 **우선순위 · 수행 위치(파일) · 수용 기준(acceptance) · 에지 소멸 리스크**를 항목마다 명시함.
> 대상 시스템: `STEP2(돌파) + RS percentile 게이트 + 저항선 재이탈 청산`, `config.py` 기존값.

---

## 0. Claude Code 사용 안내

- **모델**: 아래 **P0(진단·정합성 검증)는 Opus 권장.** 진입가 룩어헤드·역선택은 코드가 맞아 보여도 한 봉 어긋나는 종류라 추론 집약적임. 설계 확정 후 **반복 구현은 `/model`로 Sonnet 4.6 전환**(더 빠르고 한도 절약).
- **불변 규율 (모든 변경에 강제)**: §7.6에서 발견한 함정 — *조건을 조이면 평균은 좋아 보이지만 상위20건 의존도가 같이 올라간다.* 따라서 **모든 재실행은 `평균 · 중간값 · 상위20건 수익비중`을 항상 세트로 기록**한다. 평균만 보고 채택 금지.
- **출처 태그**: `[C]` Claude, `[G]` Gemini, `[X]` ChatGPT. `[C+G+X]` = 만장일치.
- **분류**: `P0` 블로커(통과 못 하면 나머지 무의미) · `P1` 신뢰도 보정 · `P2` 실험(결과로 판정) · `운용` Phase 2 실거래 · `모순` 사용자가 코드 보고 판정.

---

## 1. 3-모델 합의 매트릭스

| # | 항목 | C | G | X | 합의 | 분류 |
|---|---|:--:|:--:|:--:|---|---|
| 1 | 진입·청산 **체결 시점 현실화**(T종가→T+1시가) | ● | ●(청산측) | ●(진입측) | **만장일치** | P0-1 |
| 2 | **역선택/상한가 미체결** 보정 | ●(슬리피지) | ●(역선택) | ●(갭/체결) | **만장일치** | P0-2 |
| 3 | **비용·슬리피지** 현실화(저유동성 차등, 2026 세금) | ● | ● | ● | **만장일치** | P0-3 |
| 4 | **중간값 음수 = 우측꼬리 의존** 구조 이해 | ● | ●(암시) | ● | **만장일치** | 운용 |
| 5 | **포지션 2~5% + 링펜스(§7.3)** 소액 시작 | ● | ● | ● | **만장일치** | 운용 |
| 6 | 파라미터 튜닝 중단 타당 | ● | ● | ● | **만장일치** | (확인) |
| 7 | 매매신호=가격/거래량/RS only, 정성데이터는 판단보조 | ●(암시) | △(아래 모순) | ● | 2/3 | 운용 |
| 8 | **생존편향/상폐 종목** 보정 | ●(낮게 봄) | – | ●(최우선) | 2/3, **우선순위 상향** | P1-1 |
| 9 | **Equity curve/MDD/연속손실** 포트폴리오 검증 | △(CI로) | – | ● | ChatGPT 주도 | P1-2 |
| 10 | **부트스트랩 CI + OOS 봉인(2019–21)** | ● | – | – | Claude 단독, 방법론 타당 | P1-3 |
| 11 | **재무 게이트**(영업이익 적자 제외) | – | ●(추가) | △(반대) | **모순** | P2-1 |
| 12 | **시장국면 필터 = 사이징 조절용**(진입필터 금지) | – | – | ● | ChatGPT 단독 | P2-2 |
| 13 | **포트폴리오 리스크 규칙**(일일 진입 한도/테마 중복/손실 한도) | – | – | ● | ChatGPT 단독 | P2-3 |
| 14 | **전략 정체성 = 단기 돌파 모멘텀**(스윙 아님) | ● | – | ● | 2/3 | (인식) |

---

## 2. P0 — 블로커 (만장일치, Opus 권장)

> 셋 다 같은 곳을 가리킴: **짧은 보유기간 × 저유동성 폭등주**. P0가 통과 못 하면 +0.90%/+0.45%는 아티팩트.

### P0-1. 체결 시점 현실화 `[C+G+X]`
**문제**: 신호는 T 종가·당일 전체 거래량으로 판정되는데, 실거래(Phase 1 리포트 06:30 → 사람이 T+1 진입)는 빨라야 **T+1 시가**. 백테스트가 T 종가 진입/청산이면 (a) 한 세션 늦은 구조 + (b) 돌파주 T종가→T+1시가 갭을 통째로 못 먹음. 보유 중간값 1일이라 하루 차이가 에지를 통째로 날릴 수 있음. (방증: §6 2차 "확인 1일 지연" 시 악화 = 에지가 신호 직후 첫 봉에 몰려 있다는 신호.)

**수행 위치**: `backtest/engine.py` (진입가·청산가가 어느 봉의 어느 가격인지 먼저 감사).

**작업**:
1. 현재 엔진의 진입/청산 체결 가격 가정을 명시적으로 확인(주석 또는 로그로 출력).
2. 다음 시나리오를 분리 실행해 비교(`[X]` A/B/C/D):
   - A: 신호일 T 종가 진입 (현행 추정)
   - B: T+1 시가 진입 / 청산신호 T → T+1 시가 청산
   - C: T+1 불리한 가격(고가 근사) 진입 — 추격매수 스트레스
   - D: T+1 VWAP 근사 진입
3. 갭상승 후 진입 성과 / 갭하락 시 진입 제외(신호 폐기) 효과도 같이 측정.

**수용 기준**: **B안(T+1 시가)** 기준으로 P0-3 비용 반영 후 ⓐ 평균 > 0 유지, ⓑ 모든 연도(2019–2025) 평균 양수 유지, ⓒ 중간값·상위20건 비중 함께 기록. → B에서 무너지면 전략 실전성 낮음으로 판정.

---

### P0-2. 역선택 / 상한가 미체결 보정 `[C+G+X]` ★최우선 신규 통찰
**문제 (Gemini 역선택 프레이밍)**: 미체결이 **편향**돼 있음 — 백테스트 효자(진입 시점 상한가 잠김)는 실거래에서 못 사고, 가짜 돌파(자유 체결)만 계좌에 담김. 랜덤 슬리피지보다 나쁘며 에지 부호를 뒤집을 수 있음.

**모순 해소 (시점 분리)**:
- **진입 시점** 상한가 잠김 → **제외**(체결 불가). `[G]`
- **보유 중** 상한가 → **유지**(101670처럼 진입 후 상한가는 정당한 수익원, 제외하면 수익원 제거). `[X]`

**수행 위치**: `backtest/engine.py` 진입 로직 + `backtest/data_cache.py`(상한가 판정 컬럼).

**작업**:
1. 신호일 종가가 그 날 가격제한폭 상단(전일종가×1.30 근사, 잠김 판정 = 종가==상한가 & 매도호가 부재 근사: 당일 거래대금 극소/장중 고가==종가 등)인 진입을 **제외**.
2. **단, 보유 기간 중 상한가는 절대 제외하지 않음.** 101670 케이스(진입 8-31 → 이후 상한가)는 살아남아야 정상.
3. 거래정지일에 청산신호가 걸린 거래는 "최악가격(다음 거래 가능일 시가 등)" 시뮬레이션. `[X]`

**수용 기준**: 제외 전후 `거래수 · 평균 · 중간값 · 상위20건 비중` 비교. 제외로 사라진 수익이 "어차피 미체결이라 실현 불가능했던" 수익임을 명시적으로 확인(= 환상 수익 제거가 목적).

---

### P0-3. 비용·슬리피지 현실화 `[C+G+X]`
**문제**: 현행 왕복 0.3% 일괄 차감은 **에지가 사는 저유동성 종목에서 과소평가**. **2026년 매도세만으로 코스피 0.20%(거래세 0.05%+농특세 0.15%), 코스닥 0.20%** + 편도 수수료 ~0.015% → 슬리피지 0이어도 이미 ~0.23%. 0.3% 안에 슬리피지로 남는 건 ~0.07%인데 비현실적.

**수행 위치**: `backtest/engine.py` 비용 모델(또는 `run_signal_backtest.py` 후처리).

**작업**:
1. 세금: 매도측 0.20%(2026 기준) 반영. *백테스트 과거 구간은 당시 세율과 다를 수 있으나, 실거래 추정은 2026 기준으로 보수화.*
2. **거래대금 분위수별 차등 슬리피지** 도입(편도 예시, 진입·청산일 각각의 거래대금 기준):
   - 상위 유동성 분위: 0.1%
   - 중간 분위: 0.3%
   - 하위 분위(저유동성): 0.7%+
3. (선택) 주문금액 / 진입일 거래대금 비율로 시장충격 근사 추가. `[X]`

**수용 기준**: 차등비용 반영 후에도 평균 > 0 유지 여부. (이미 §7.1에서 일괄 0.3%로 +0.90%→+0.45% 반토막 났으므로, 차등비용에서 얼마나 더 깎이는지가 관건.)

---

## 3. P1 — 신뢰도 보정 (2/3 합의 + 방법론 타당)

### P1-1. 생존편향 정량화 `[C+X]` (우선순위 상향)
**문제**: `FinanceDataReader.StockListing("KRX")`는 **현재 상장 기준** → 기간 중 상폐 종목 누락. 이 전략은 정확히 **상폐·관리종목 위험군 소형주**를 많이 잡으므로 영향이 큼. 8년 +0.99%가 실제보다 낙관 편향일 수 있음.

**수행 위치**: `backtest/data_cache.py`(유니버스 구성).

**작업 (완전 제거가 아니라 편향 크기 추정이 목적)**:
1. 연도별 상장목록 스냅샷 확보, 또는 상폐 종목 리스트 별도 수집 후 유니버스에 포함.
2. 관리종목 / 거래정지 이력 태깅 → 실전 불가 종목 필터링.
3. SPAC · 우선주 · 리츠 · ETF · ETN 분류 후 제외 여부 실험.

**수용 기준**: 상폐 종목 포함 시 8년 평균이 얼마나 내려가는지 상·하한 제시. (예: "+0.99% → +0.X%" 범위.) → 블로커는 아니나 실전 기대치를 이 보정 후 값으로 재설정.

---

### P1-2. 포트폴리오 레벨 검증 `[X]`
**문제**: 거래단위 평균 +0.99%는 계좌 곡선이 아님. 실전 지속 가능성은 MDD와 연속손실로 결정됨(승률 41%면 5연패 흔함).

**수행 위치**: §7.2 5%비중 연속 시뮬 위에서 산출(`run_signal_backtest.py` 또는 신규 스크립트).

**산출 지표**: CAGR · MDD · 최대 연속손실 횟수 · 월별 승률 · 월별 거래수 · 현금 비중.

**수용 기준**: **MDD가 링펜스 손실한도(전체자산의 10%, §7.3) 안에 들어오는지.** 넘으면 비중·서브계좌 크기 재설계.

---

### P1-3. 부트스트랩 CI + OOS 봉인 `[C]`
**문제**: ⓐ 분포가 극단적 우측편향이라 평균 점추정은 소수 꼬리에 좌우됨(신뢰구간 없음). ⓑ 최종 조합을 2023–2025에서 고르고 2019–2025로 "확인"했는데 후자가 전자를 **포함**하므로 순수 OOS 아님.

**작업**:
1. **블록 부트스트랩**(월 블록 리샘플)으로 평균수익률 95% CI 산출. → 하한이 P0-3 비용 반영 후에도 > 0인지.
2. **2019–2021을 봉인**하고 모든 의사결정을 2022–2025에서 종료한 뒤, 봉인 구간에 **딱 한 번** 검증.

**수용 기준**: CI 하한 > 0(비용 후) **그리고** 봉인 구간 평균 양수.

---

## 4. P2 — 실험 (결과로 판정. 개선 확정 아님)

> STEP1 교훈: **게이트를 얹으면 수익 종목까지 걸러 기대값이 깎일 수 있다.** 아래는 전부 "넣어보고 §7.6 규율로 판정"하는 실험.

### P2-1. 재무 게이트 실험 `[G]` ↔ `[X]` (모순 → 실험으로 판정)
**내용**: "직전 4분기 누적 영업이익 < 0 종목 제외" 게이트를 STEP2+RS 위에 얹어 backtest (CAN SLIM의 C/A). 데이터는 `collectors/dart.py` 재무지표 활용 가능.

**모순 메모**: `[G]`는 추가 주장, `[X]`는 정성데이터를 신호에 섞지 말라 주장. → **하드 재무수치는 재현·백테스트 가능**하므로 ChatGPT 우려(뉴스/AI 해석의 비재현성)와 충돌하지 않음. 단 개선 확정이 아니라 실험.

**⚠️ 룩어헤드 주의**: 과거 시점 재무는 **공시 시차(point-in-time)** 반영 필수. 발표 전 분기 실적을 그 분기 중 신호에 쓰면 룩어헤드. "신호일 기준 이미 공시된 가장 최근 재무"만 사용.

**수용 기준**: 평균·중간값·상위20건 비중 모두 확인. STEP1처럼 평균(승)이 깎이면 기각.

---

### P2-2. 시장국면 필터 = 비중 조절용 `[X]`
**내용**: KOSPI/KOSDAQ 60일선 기준을 **진입필터가 아니라 자본노출 조절**로:
```
KOSPI>60일선 AND KOSDAQ>60일선  → 정상 비중
둘 중 하나만                    → 절반 비중
둘 다 60일선 아래               → 신규 진입 축소/중단
```
**⚠️**: 반드시 backtest. **진입필터로 넣으면 STEP1처럼 큰 수익 종목을 걸러낼 위험.** 핵심은 "신호 차단"이 아니라 "노출 조절".

**수용 기준**: 사이징 조절 버전의 equity curve(MDD↓) vs 무필터 비교. CAGR 손실 대비 MDD 개선이 유의한지.

---

### P2-3. 포트폴리오 리스크 규칙 `[X]`
**내용**: 종목당 최대 비중 / 일일 신규 진입 최대 개수 / 동일 테마·업종 중복 제한 / 동시보유 최대 / 일일 손실 한도 / 월간 손실 한도.

**수용 기준**: 규칙 적용 전후 MDD·연속손실 비교. 기대수익을 크게 깎지 않으면서 꼬리리스크를 줄이는지.

---

## 5. 운용 (Phase 2 실거래 합의안)

> **만장일치**: 지금은 "돈을 크게 넣을 전략"이 아니라 "작은 자금으로 체결·슬리피지·생존편향을 검증할 전략". `[C+G+X]`

### 5.1 자본 배분 (링펜스, §7.3) `[C+G+X]`
```
전체 시드            1,000만원
전략 서브계좌        100~200만원 (10~20%, 최대손실=전체의 10~20%로 제한)
종목당 비중          3~5%
최대 동시보유        10~20종목
더블(100%) 달성 시   초과분 출금, 원금 리셋
```
중간값이 음수인 우측꼬리 분포라, 큰 비중은 복리가 아니라 **시퀀스 운**(§7.2). 링펜스가 구조적으로 이 문제를 해소.

### 5.2 가상매매(페이퍼) 로그 — Phase 2 전 1~2달 `[G+X]`
매일 리포트 보며 "내가 오늘 종가/내일 시가에 **실제로 체결**할 수 있었나"를 기록:
```
신호 발생 시각 / 종목명·티커 / 신호일 종가
실제(가상) 매수가 / 실제(가상) 매도가
백테스트 진입가와의 차이
체결 실패 여부 (상한가 잠김 등)
호가 스프레드 / 매수 당시 거래대금
관련 뉴스·공시
사람이 매수 보류했는가 / 보류 사유
```
이 로그가 P0-1·P0-2의 실거래 검증 데이터가 됨. 보류 사유별 성과 비교 → 추후 필터 후보.

### 5.3 정보 레이어 분리 유지 `[X]` (현 설계 §10 유지)
```
매매 신호   : 가격 / 거래량 / RS  (전략 불변)
판단 보조   : 뉴스(RSS) / 공시(DART) / 재무지표 / AI 코멘트
```
AI 코멘트·뉴스 해석을 **신호에 직접 편입 금지**(비재현성). 재무 하드수치는 P2-1에서 별도 게이트로만 실험.

---

## 6. 모순·미해결 (사용자가 코드 보고 판정)

1. **상한가 제외 vs 보유** (`[G]`↔`[X]`) → §P0-2에서 **시점 분리로 해소**. 단, 엔진에서 "진입이 보유 중 상한가가 아니라 *진입 시점* 잠김"을 정확히 판정하는지 코드로 확인 필요.
2. **재무 게이트** (`[G]`↔`[X]`) → §P2-1 실험으로 판정. point-in-time 룩어헤드만 주의.
3. **생존편향 우선순위** (`[C]` 중간 ↔ `[X]` 최우선) → 블로커는 아니나 **소형주 집중 전략 특성상 상향**. P1-1로 "편향 크기 정량화" 후 실전 기대치를 그 값으로 재설정.
4. **3개 모델 공통 맹점 가능성**: 셋 다 코드를 직접 보지 못하고 설계서로 추론함. 특히 **진입/청산 체결가 가정(P0-1)**은 세 모델 모두 "추정"이므로, 엔진 코드 1차 감사가 사실상 모든 결론의 전제. → **P0-1을 가장 먼저.**

---

## 7. 실행 순서 요약

```
P0-1  엔진 체결가 감사 → T+1 시가 강제 재검증        (Opus)  ← 모든 것의 전제
P0-2  진입 시점 상한가 잠김 제외 (보유 중 상한가 유지) (Opus)
P0-3  2026 세금 + 거래대금 분위 차등 슬리피지         (Opus)
  └─ 게이트: 위 3개 후에도 평균>0, 전 연도 양수 유지하면 통과
P1-1  생존편향 정량화 (상폐 포함, 편향 크기 추정)
P1-2  equity curve / MDD / 연속손실 (MDD ≤ 링펜스 한도)
P1-3  부트스트랩 CI + 2019–21 봉인 OOS 1회
P2    재무게이트·국면필터·포트폴리오규칙 = 실험(§7.6 규율로 판정)
운용  링펜스 소액 + 1~2달 가상매매 로그 → Phase 2
```

**한 줄 결론**: 에지는 있어 보이지만, P0 세 항목(체결 타이밍·역선택·차등비용)이 전부 같은 급소 — *짧은 보유 × 저유동성 폭등주* — 를 친다. **P0를 통과하면 진짜고, 무너지면 지금까지의 양의 기대값은 백테스트 아티팩트다.**
